using Microsoft.AspNetCore.Builder;

namespace StravaExporter.Common.Extensions;
public static class StravaEndpoints
{
    public static void MapActivitiesEndpoints(RouteHandlerBuilder group)
    {
        // group = builder.MapGroup("activities");
        
    }
}